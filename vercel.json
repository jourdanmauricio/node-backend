{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "builds": [
    {
      "src": "/api/index.js && npm run migrations:run",
      "use": "@now/node"
    }
  ]
}
