{
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ],
  "builds": [
    {
      "src": "npm run migrations:run && /api/index.js",
      "use": "@now/node"
    }
  ]
}
